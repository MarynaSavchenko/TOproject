import model.Sensors.SensorCreator
import model.Sensors.ThreadSensor
import model.Server.SensorsAndTasksManager
import model.Tasks.BigFactorial
import model.Tasks.ITask
import model.Tasks.TaskType
import spock.lang.Specification

class SensorsAndTasksManagerTest extends Specification{
/*
    def "Task should not be null"(){
        given:
        SensorsAndTasksManager satm = new SensorsAndTasksManager()
        ITask task = new BigFactorial(TaskType.BIGFACTORIAL)

        SensorCreator sensorCreator = new SensorCreator(ThreadSensor.class)

        when:
        sensorCreator.start()
        satm.assignSensor(task)

        Thread.sleep(10000)
        then:
        task.result != null
    }
*/
}
